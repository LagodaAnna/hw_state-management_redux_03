import styled from 'styled-components';
import { typography, space, color, grid, layout, flexbox } from 'styled-system';

const Box = styled('div')(typography, space, color, grid, layout, flexbox);

export default Box;
